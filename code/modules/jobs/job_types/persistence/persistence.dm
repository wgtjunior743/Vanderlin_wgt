//needed pls dont remove
/datum/job/persistence
	enabled = FALSE
	allowed_races = RACES_PLAYER_ALL
	allowed_ages = ALL_AGES_LIST
	total_positions = 0
	spawn_positions = 0

//The jobs need to have their name be different than the pre-existing jobs
//can't have two miners basically, even if both have different datums
